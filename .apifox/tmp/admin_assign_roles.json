{
  "parameters": {
    "path": [
      {
        "id": "admin_id#0",
        "name": "admin_id",
        "required": true,
        "type": "integer",
        "description": "管理员ID"
      }
    ]
  },
  "requestBody": {
    "type": "application/json",
    "required": true,
    "jsonSchema": {
      "type": "object",
      "properties": {
        "roles": {
          "type": "array",
          "description": "角色名称数组（必须为已存在的角色名称）",
          "items": {
            "type": "string",
            "maxLength": 50,
            "description": "角色名称"
          }
        }
      },
      "required": [
        "roles"
      ]
    }
  },
  "responses": [
    {
      "id": 1,
      "name": "分配成功",
      "code": 200,
      "contentType": "json",
      "jsonSchema": {
        "type": "array",
        "description": "管理员已分配的角色名称数组",
        "items": {
          "type": "string",
          "description": "角色名称"
        }
      }
    }
  ]
}
